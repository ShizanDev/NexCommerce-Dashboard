DATABASE_URL=file:/home/z/my-project/db/custom.db

# ─── Email Configuration (Resend) ───────────────────────────────────
# Optional: Set these for fully automated email delivery.
# If not set, email can be configured via the Settings UI instead.
# Get a free API key at https://resend.com/api-keys
#
# RESEND_API_KEY=re_xxxxxxxxxxxxx
#
# Default from address (use onboarding@resend.dev without DNS setup).
# For custom domains, verify your domain in Resend first.
#
# EMAIL_FROM=onboarding@resend.dev
